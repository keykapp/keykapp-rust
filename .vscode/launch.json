{
  "version": "0.2.0",
  "configurations": [
      // Running executables
      {
          "type": "lldb",
          "request": "launch",
          "name": "Run Keykapp",
          "program": "${workspaceFolder}/keykapp/.build/debug/keykapp",
          "args": [],
          "cwd": "${workspaceFolder}/keykapp/",
          "preLaunchTask": "swift-build"
      },
  ]
}